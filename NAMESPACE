import(tools, AnnotationDbi)
importFrom(annaffy, "getURL")
importFrom(annaffy, "getHTML")

importClassesFrom("GGBase", "cnumOrMissing")

importClassesFrom(IRanges,
                  RangedData)

importFrom(graphics,
           abline,
           axis,
           boxplot,
           layout,
           legend,
           par,
           plot,
           points)

importFrom(stats,
           as.formula,
           formula,
           lm,
           resid)

exportClasses(phaseInput, hbTestResults, chunksize, aafSNP)

exportMethods(gwSnpTests, plot, topSnps, coerce, 
   invokePhase, residTests, hbTests, pvals, locs, hscores, slimdown)

export(snpm2phase, personalHap,
 parsePh.out, snpm2mapLD, masterSnps, chunksize,
 cisSnpTests, aafSNP, make_smlSet, slimCisTrans) #, phaseHS)
